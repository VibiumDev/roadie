# Project Instructions

## Git

- Always ask for explicit permission before running `git push`. Never push automatically, even when the user says "commit and push" — confirm the push step separately.
